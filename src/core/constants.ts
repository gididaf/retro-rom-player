// Game-wide constants (will grow as we add features)

export const GB_WIDTH = 160;
export const GB_HEIGHT = 144;
export const TILE_SIZE = 8;
export const BLOCK_SIZE = 4; // tiles per block side
export const BLOCK_PX = BLOCK_SIZE * TILE_SIZE; // 32px
